# AGENTS.md

本仓库给 Agent / 协作者的开发约定。后续改动须遵守本文与 [git-commit.md](git-commit.md)。先看第 1 节网站定位，再写功能。

## 1. 网站定位

本站是《吹响吧！上低音号》（響け！ユーフォニアム）介绍站，不是通用落地页，也不是技术栈演示。文案、配色、交互和素材都要服务「把这部作品讲清楚」。

### 要做成什么

面向观众的系列介绍：用全屏切页呈现北宇治高校吹奏乐部与京都动画这部作品。首页里「技术栈 / 展示区 / 准备开始」仍是占位，应逐步换成正式介绍，不要再往模板方向加功能。

主要内容：

1. **作品**：TV 三季、剧场版、特别篇、最终乐章等，用封面或主视觉做系列介绍。
2. **角色**：部员与相关人物（立绘、声部、年级等）。
3. **乐器**：部里出现的乐器介绍。

视觉沿用北宇治青金：主色 `#01acc6`，辅色 `#fff830`，深底 `ink` / `deep` / `panel`。黄只做点缀（标签、描边、按钮），不要整页铺黄。

交互是全屏 seam wipe 切页；桌面滚轮 / 键盘与手机滑动都要能用。

本站是个人介绍页，不要写成官方站，不要冒充京都动画或官方宣发。

### 素材

正式图放在 `src/assets/`，优先复用已有资源，不要重复下载：

- `covers/`：系列封面（`landscape/`、`portrait/` 为选用图）
- `characters/`：角色立绘
- `instruments/`：乐器透明图
- `_official` / `_tmp` / `_preview` / `_pairs`：取材或对照用，默认不要直接挂到页面

## 2. 目录规范

根目录只放工程入口与约定（`index.html`、`vite.config.ts`、`AGENTS.md`、`git-commit.md` 等）。业务代码进 `src/`，看文件夹即可分辨职责：

```text
src/
  assets/          # 静态资源：封面、立绘、乐器、favicon、字体
  styles/          # 全局 CSS（Tailwind、Lenis 等）
  routes/          # 页面：TanStack 文件路由，只做组装，不堆长逻辑
  components/      # UI，按功能分子目录
    fullpage/      # 全屏滚动相关
      sections/    # 每一屏独立文件
    lenis/         # Lenis 根 Provider
  hooks/           # 可复用 React hooks
  lib/             # 无 UI 纯函数
  constants/       # 文案、区块 id 等常量
```

- 页面路由文件只负责组装与接线。
- 新增功能优先落在对应文件夹，不要在根目录或无关目录散落文件。
- `src/routeTree.gen.ts` 由插件生成，勿手改。

## 3. 文件长度与封装复用

- 单文件过长（约超过 150～200 行，或已有两套独立职责）必须拆分。
- 出现第二次相同结构（区块外壳、导航、工具函数等）必须抽成组件 / hook / `lib`，禁止复制粘贴。
- 可复用逻辑放 `hooks/` 或 `lib/`；可复用 UI 放 `components/` 对应子目录。

## 4. 开发前先查复用

动手写新代码前：

1. 搜索 `src/` 是否已有同类组件、hook、常量。
2. 确认当前依赖（Lenis、TanStack Router、Tailwind 等）是否已能覆盖需求。
3. 能复用就复用，不重复造轮子。

## 5. 浏览器验收（改 UI 必做）

凡改到 UI、布局、路由、滚动、favicon 等可视化行为：

- 必须启动开发服务器并**真实打开浏览器**操作验收。
- 禁止只靠读代码或单张截图就宣布完成。
- 至少覆盖：主路径、相关路由/状态面、空态或边界（若涉及）。
- **必须同时验收桌面端与移动端**，不能只看宽屏。
- 未跑通验收路径不算完成。
- 验收结束后必须关掉本次启动的开发服务器等后台进程，不要让 Node 进程残留。

### 5.1 移动端布局（每次开发都要考虑）

改界面时默认按小屏先排，再用 `sm` / `md` 增强，不要只按桌面稿再硬缩。

- 布局在窄屏（至少一种常见宽度，如 390×844）下不能横向溢出、不能被裁切成不可读。
- 正文与标题字号在手机上仍可读；可点击区域大约不小于 44×44。
- 预留刘海 / Home 指示条安全区（`viewport-fit=cover` 与 `safe-area-inset-*`）。
- 全屏切页、触摸滑动、末页 footer 等手势在移动端也要能用。
- 验收时除桌面视口外，至少用浏览器把视口缩到手机尺寸（或设备模式）走一遍主路径。

## 6. 提交与推送

修改完成且浏览器验收通过后：

1. 运行 `git status` / `git diff`，确认未带入密钥、日志或无关文件。
2. **一并暂存用户已有改动**（例如 `git-commit.md`、`src/assets` 中的资源）与本次实现，不要只提交 Agent 新写的文件。
3. 严格按 [git-commit.md](git-commit.md) 的 Conventional Commits 写说明；文档与功能尽量分开提交。
4. 提交后推送到已跟踪的 `origin`；禁止对 `main` / `master` 使用 `push --force`。
5. 不修改 git config；不跳过 hook。

## 7. 写完后关闭后台服务

每次任务结束（含浏览器验收、提交推送之后）必须关掉本任务启动的后台进程：

- 开发服务器（`npm run dev` / Vite）、预览、一次性 Node 脚本、打包监听等。
- 不要把 Node.js 进程留在后台堆着；写完就停，下一轮需要再重新启动。
- 先查 terminals / 监听端口，再结束对应进程（含子进程），确认端口已释放。
- 不要误杀编辑器、TypeScript 语言服务等 Cursor 自身的 Node 进程。
